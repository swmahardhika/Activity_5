<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginconstraintlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Aplikasi Login"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginConstraintLayout">
        <activity android:name=".ActivityLihatData"
            android:parentActivityName=".Home_Activity"></activity>
        <activity android:name=".Home_Activity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".DaftarActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ActivityKedua"
            android:label="@string/label12"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>